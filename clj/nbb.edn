;; nbb reads this file. It does NOT read bb.edn, and the
;; .cljs here are run as bare `nbb <file>`, so a coordinate declared only
;; there is invisible to them.
;;
;; :paths and every sha below are COPIED from bb.edn, not chosen here.
{:paths ["src" "test"]
 :deps {
        ;; Declared HERE and not only in bb.edn: nbb reads nbb.edn and does NOT
        ;; read deps.edn or bb.edn, and the .cljs in this repo are run as
        ;; bare `nbb <file>`. A coordinate they cannot see is not a
        ;; declaration. sha and :paths are COPIED from bb.edn.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}}}
